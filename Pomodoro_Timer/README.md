# Pomodoro Timer

A focused Pomodoro timer with session history and customizable modes.

## Live Demo

- https://pomodoro-hasham.netlify.app/

## Run Locally

```powershell
npm install
npm run dev
```

Then open the local URL printed in the terminal (typically `http://localhost:5173`).
